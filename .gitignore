# OS
.DS_Store
**/.DS_Store
Thumbs.db

# Office / LibreOffice locks & temp
.~lock.*#
*.tmp
*.docx

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Build output / local
dist/
.cache/
*.log
.env

# Claude Code local settings & preview shim
.claude/settings.local.json
.claude/launch.json
.mcp.json
server.py

# stray off-spec scaffold (not used — the vanilla build lives at the root)
poonno-site/

# Raw (unoptimized) source models — huge (>100MB), GitHub-rejected. The site
# loads the optimized copies in assets/models/opt/ instead (those ARE committed).
assets/models/*.glb

# Local agent / tooling caches (not part of the website)
.agents/
.claude/
.extract-design-system/
